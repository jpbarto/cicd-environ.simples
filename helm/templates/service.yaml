apiVersion: v1
kind: Service
metadata:
  name: {{ include "simples.fullname" . }}
  labels:
    {{- include "simples.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simples.selectorLabels" . | nindent 4 }}
